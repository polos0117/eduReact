// ===== 드릴 04: 함수를 값으로 — "함수도 서랍에 넣을 수 있다" =====
// 실행 방법: node drills/04-functions-as-values.js
// 오늘의 핵심: 함수도 숫자/문자열/배열처럼 "값"이다.
//             변수에 담고, 인자로 넘기고, 이름표를 여러 개 붙일 수 있다.

// ---------------------------------------------------------
// [문제 1] 함수를 변수에 담고, 이름표 더 붙이기
// (a) 단어를 받아 뒤에 '!'를 붙여 반환하는 화살표 함수 shout를 만드세요.
// (b) const yell = shout; 로 이름표를 하나 더 붙인 뒤, yell로 호출해보세요.
//     (어제 배운 const b = a 와 같은 일이 함수에도 일어나는지 확인)
// ---------------------------------------------------------

// TODO: shout 만들기
// TODO: yell 만들기
const shout = (word)=> word+"!"; 
const yell = shout;
console.log('문제1:', shout('안녕'), yell('간다'));
// 기대 출력 → 문제1: 안녕! 간다!

// ---------------------------------------------------------
// [문제 2] 실험: fn 과 fn() 의 차이 — 오늘의 최중요 실험!
// 실행 전에 두 줄의 출력을 각각 예측해서 적으세요.
// ---------------------------------------------------------
function now() {
    return '지금이야';
}

// 예측 (a): function now() {return '지금이야';}
// 예측 (b): 지금이야
console.log('문제2a:', now);
console.log('문제2b:', now());

// 관찰 후 한 문장으로:
// now 는 이름 이고, now() 는 호출 이다.

// ---------------------------------------------------------
// [문제 3] 함수를 "받는" 함수 만들기 — test()의 입장 되어보기
// 함수 fn을 받아서 그 fn을 두 번 실행해주는 함수 twice를 만드세요.
// 그다음 twice에 "'반복!'을 출력하는 화살표 함수"를 넘겨 호출하세요.
// ---------------------------------------------------------

// TODO: twice 만들기
// TODO: twice(...) 호출하기 — 인자로 화살표 함수를 직접 넘길 것
function twice(fn) {
    fn();
    fn();
}
twice(()=>console.log('반복!'));
// 기대 출력 →
// 반복!
// 반복!

// 생각해볼 것: 넘긴 화살표 함수를 실행한 건 "나"인가, "twice"인가?
// 답: twice
// (test('이름', () => {...}) 도 같은 구조 — 화살표 함수를 실행하는 건 내가 아니라 Vitest다)

// ---------------------------------------------------------
// [문제 4] map / filter 에 함수 넘기기
// (a) nums의 모든 수를 2배로 만든 새 배열 doubled를 만드세요. (map)
// (b) nums에서 10보다 큰 수만 남긴 새 배열 big을 만드세요. (filter)
// ---------------------------------------------------------
const nums = [3, 12, 7, 20];

// TODO: doubled 만들기
// TODO: big 만들기
const doubled = nums.map(num=>num*2);
const big = nums.filter(num=> num > 10);
console.log('문제4:', doubled, big);
// 기대 출력 → 문제4: [ 6, 24, 14, 40 ] [ 12, 20 ]

// ---------------------------------------------------------
// [문제 5] 내 코드에서 찾기 (코드 작성 없음)
// src/components/Todo/TodoApp.jsx 를 열고:
// (a) 함수를 "호출하지 않고 값으로 전달만" 하는 곳을 두 군데 찾아 적으세요.
//     (힌트: JSX의 프롭들. 괄호가 없다는 게 포인트!)
// (b) 그 함수들을 실제로 "호출"하는 건 누구일까요?
// 답: (a)onAddTodo={addTodo}, onToggleTodo={toggleTodo}, onRemoveTodo={removeTodo}, onFilterChange={setFilter}
// 답: (b) 건네받은 자식 컴포넌트
// ---------------------------------------------------------
