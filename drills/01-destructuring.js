// ===== 구조 분해 드릴 =====
// 실행 방법: 터미널에서  node drills/01-destructuring.js
// 각 문제의 TODO 자리에 코드를 쓰고, 실행 결과가 "기대 출력"과 같으면 통과!

// ---------------------------------------------------------
// [문제 1] 긴 버전 → 구조 분해로 줄이기
// 아래 세 줄을 "구조 분해 한 줄"로 바꿔보세요. (세 줄은 지우기)
// ---------------------------------------------------------
const[first, second] = ['사과', '바나나'];
console.log('문제1:', first, second);
// 기대 출력 → 문제1: 사과 바나나

// ---------------------------------------------------------
// [문제 2] 반대 방향: 구조 분해 → 긴 버전으로 풀어 쓰기
// 아래 구조 분해 한 줄을 "인덱스로 꺼내는 세 줄"로 바꿔보세요.
// (읽는 방향, 쓰는 방향 둘 다 자유로워야 진짜 아는 거예요)
// ---------------------------------------------------------
const drinks = ['커피', '맥주'];
const morning = drinks[0];
const evening = drinks[1];  

console.log('문제2:', morning, evening);
// 기대 출력 → 문제2: 커피 맥주

// ---------------------------------------------------------
// [문제 3] 객체 구조 분해: 이름이 기준!
// todo 객체에서 text와 completed만 구조 분해로 꺼내보세요.
// 그다음 실험: 꺼내는 이름을 일부러 { txt } 로 틀리게 바꿔서
// 실행해보고, 뭐가 출력되는지 관찰한 뒤 다시 고치세요.
// ---------------------------------------------------------
const todo = { id: 1, text: '우유 사기', completed: false };

// TODO: 여기에 구조 분해 한 줄
const {text, completed} = todo;
console.log('문제3:', text, completed);
// 기대 출력 → 문제3: 우유 사기 false
// undefined false 뜨네

// ---------------------------------------------------------
// [문제 4] useState 스타일: 함수가 배열을 반환
// getMinMax는 [최솟값, 최댓값] 배열을 반환합니다.
// 반환값을 구조 분해로 받되, 변수 이름은 본인 마음대로 지어보세요.
// (배열 구조 분해에서 이름은 내가 짓는다 — useState와 같은 원리)
// ---------------------------------------------------------
function getMinMax(numbers) {
    return [Math.min(...numbers), Math.max(...numbers)];
}

// TODO: 여기에 구조 분해 한 줄 (getMinMax([3, 7, 1, 9]) 사용)
const[minVal, maxVal] = getMinMax([3,7,1,9]);
// TODO: console.log('문제4:', 내가지은이름1, 내가지은이름2);
// 기대 출력 → 문제4: 1 9
console.log('문제4:', minVal, maxVal);